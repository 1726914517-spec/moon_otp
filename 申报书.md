## moon_otp 项目申报书

- 参赛者：朱天池
- 联系方式：1415333198@qq.com

**1. 项目名称**

moon_otp：MoonBit 一次性密码库（HOTP / TOTP）

**2. 项目简介**

moon_otp 用纯 MoonBit 实现两步验证（2FA）中最通用的一次性密码标准：事件型 HOTP（RFC 4226）和时间型 TOTP（RFC 6238），并补齐全部依赖——HMAC（RFC 2104）、SHA-1/256/512（FIPS 180-4）、RFC 4648 Base32、随机密钥生成，以及 Google Authenticator 事实标准的 otpauth:// URI 构建与解析。申报前已在 mooncakes.io 检索 TOTP/HOTP/OTP/Base32/otpauth 关键词，均无重合项目。

**3. 项目方向与通用性说明**

方向为开发者工具 / 工具库。一次性密码是互联网登录与账号安全的基础能力，绝大多数提供身份验证的服务都会用到；RFC 4226/6238 是各验证器 App、云厂商和硬件令牌共同遵循的标准，互操作性强。库零外部依赖，可编译到 wasm、native、js 各后端，任何 MoonBit Web 服务、浏览器 WASM 应用或命令行工具都能直接引入。

**4. 预期使用场景（3 个）**

1. **网站登录两步验证**：MoonBit 后端在用户绑定阶段生成随机密钥与 otpauth 链接（用户扫码导入验证器 App）；登录时用 `totp_now` 校验提交的 6 位码，并接受前后各一个时间步的时钟偏差。
2. **纯前端 WASM 应用**：浏览器端 MoonBit/WASM 应用在本地生成密钥、计算和校验验证码，无需调用宿主 JavaScript 加密库，可离线运行。
3. **运维与命令行工具**：通过 CLI 的 gen/now/hotp/uri 子命令为内部系统批量生成密钥与二维码内容、查看当前 TOTP、按计数器生成 HOTP，用于服务器登录和账号交接。

**5. 拟实现的核心功能**

- RFC 4648 Base32 编解码（标准与 Extended Hex 字母表，padding 可选且严格校验）；
- SHA-1/256/512 哈希与 HMAC 统一封装；
- HOTP：8 字节大端计数、动态截断、位数与零填充可配；
- TOTP：时间步长、位数、算法可配，提供当前时间便捷接口；
- 基于平台 CSPRNG 的随机密钥生成；
- otpauth://totp URI 的构建与完整解析；
- CLI 四个子命令 gen/now/hotp/uri；统一错误类型 OtpError。

**6. 项目性质**

原创项目。依据公开 RFC/FIPS 标准从零实现，未移植或复制任何开源项目代码；SHA-512 轮常量与初始值曾与 Mozilla NSS 公开源码逐项交叉核对。本项目许可证为 Apache-2.0。

**7. 移植说明**

不适用（非移植项目）。参考标准：RFC 2104/4226/4648/6238、FIPS 180-4，均为公开技术规范而非代码。

**8. GitHub 仓库链接**

https://github.com/1726914517-spec/moon_otp

含 15 个有效 commit，依次覆盖脚手架、Base32、三种哈希、HMAC、HOTP、TOTP、密钥生成、otpauth、CLI、示例、CI、文档等阶段，无空提交或拆分凑数。已发布 mooncakes 包 `1726914517-spec/moon_otp@0.1.0`；32 个测试全部通过。
