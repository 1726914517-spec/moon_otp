## moon_otp 项目申报书

**基本信息**

- 项目名称：moon_otp：MoonBit 一次性密码库（HOTP / TOTP）
- 参赛者：朱天池
- 联系方式：1415333198@qq.com
- GitHub 仓库链接：https://github.com/1726914517-spec/moon_otp
- 项目方向：MoonBit 一次性密码基础库 / 账号安全基础设施（工具库 / 开发者工具方向）
- 是否为移植项目：否

**项目简介**

moon_otp 用纯 MoonBit 实现两步验证（2FA）中最通用的一次性密码标准：事件型 HOTP（RFC 4226）和时间型 TOTP（RFC 6238），并补齐全部依赖——HMAC（RFC 2104）、SHA-1/256/512（FIPS 180-4）、RFC 4648 Base32、随机密钥生成，以及 Google Authenticator 事实标准的 otpauth:// URI 构建与解析。已知 Q30399/moonvault 含部分 TOTP 功能、cc06b/mooncry 含 HOTP/TOTP 生成函数，本项目定位为端到端专用 OTP 库，补齐其缺失的 otpauth 解析、Base32、校验与命令行工具，具体关系见“与已有项目关系说明”。

**通用性说明**

一次性密码是互联网登录与账号安全的基础能力，绝大多数提供身份验证的服务都会用到；RFC 4226/6238 是各验证器 App、云厂商和硬件令牌共同遵循的标准，互操作性强。库零外部依赖，可编译到 wasm、wasm-gc、native、js 各后端，任何 MoonBit Web 服务、浏览器 WASM 应用或命令行工具都能直接引入。

**预期使用场景（3 个）**

1. **网站登录两步验证**：MoonBit 后端在用户绑定阶段生成随机密钥与 otpauth 链接（用户扫码导入验证器 App）；登录时用 `totp_now` 校验提交的 6 位码，并接受前后各一个时间步的时钟偏差。
2. **纯前端 WASM 应用**：浏览器端 MoonBit/WASM 应用在本地生成密钥、计算和校验验证码，无需调用宿主 JavaScript 加密库，可离线运行。
3. **运维与命令行工具**：通过 CLI 的 gen/now/hotp/uri 等子命令为内部系统批量生成密钥与二维码内容、查看当前 TOTP、按计数器生成 HOTP，用于服务器登录和账号交接。

**核心功能范围**

- RFC 4648 Base32 编解码（标准与 Extended Hex 字母表，padding 可选且严格校验）；
- SHA-1/256/512 哈希与 HMAC 统一封装；
- HOTP：8 字节大端计数、动态截断、位数与零填充可配（RFC 标准范围 1..8 位），并提供 RFC 4226 §7.4 的计数器重同步；
- TOTP：时间步长、位数、算法可配，提供当前时间接口与带时钟容差的 `totp_verify`；
- 基于平台 CSPRNG 的随机密钥生成；
- otpauth:// URI 的构建与完整解析，覆盖 totp 与 hotp 两种类型（含 counter 参数）；
- Steam Guard 五位自定义字母表令牌码；
- 一次性恢复码（备份码）生成；
- CLI 七个子命令 gen/now/hotp/uri/steam/recovery/verify；统一错误类型 OtpError。

**与已有项目关系说明**

本项目为原创项目，依据公开 RFC/FIPS 标准从零实现，未复制任何开源项目代码；SHA-512 轮常量与初始值曾与 Mozilla NSS 公开源码逐项交叉核对。许可证 Apache-2.0。

经逐项核对 **Q30399/moonvault**（密码哈希与加密套件）已发布的 0.1.0 及其 GitHub 源码：其 OTP 部分覆盖 TOTP 生成、校验和 otpauth 链接构建。moon_otp 与其不是重复实现，而是补齐其缺失或不可用的环节：

1. **otpauth 解析**：moonvault 只能构建链接（且不含 algorithm/digits/period 参数与 percent 编码）、不能解析；moon_otp 提供完整构建与解析往返（label issuer、percent 解码、参数校验）；
2. **命令行工具**：moonvault 没有，moon_otp 提供 gen/now/hotp/uri/steam/recovery/verify 七个子命令；
3. **正确的当前时间实现**：moonvault 源码中 `totp_now`/`totp_verify` 传入硬编码时间 0、不读系统时钟，实际不可用；moon_otp 基于系统时钟实现并支持可配置容差窗口；
4. **HOTP 与向量验证**：已发布的 moonvault@0.1.0 没有 HOTP 函数（其未发布 main 分支虽有 hotp，但无 RFC 向量验证）；moon_otp 完整实现 RFC 4226，并覆盖 HOTP Appendix D 与 TOTP Appendix B 三种算法的全部官方测试向量。

经核对 **cc06b/mooncry**（0.95.0，通用加密原语库，覆盖哈希、AEAD、KDF、非对称与后量子算法）：其 OTP 部分仅为 `hotp`/`totp` 及 SHA-256/512 变体的**生成**函数，不含 Base32、otpauth 构建与解析、当前时间与校验接口、Steam Guard、恢复码、计数器重同步，也没有 CLI。moon_otp 是端到端专用 OTP 方案，覆盖注册（CSPRNG 密钥生成 + otpauth）、分发（CLI）与校验（容差验证、重同步）全流程，与其互补而非重复。

此外 Tigls/mb-hmac、yyjeqhc/base32、Lfan-ke/basex 分别提供 HMAC/Base32 单组件；moon_otp 内自有实现，以保证 OTP 层对三种哈希和密钥校验的完整控制，这些基础组件不作为交付内容。

**移植或参考说明**

非移植项目，未使用 moonvault、mooncry 或其他项目的代码。参考标准：RFC 2104/4226/4648/6238、FIPS 180-4，均为公开技术规范而非代码；本项目许可证 Apache-2.0。

**仓库与测试情况**

- 仓库：https://github.com/1726914517-spec/moon_otp
- 含 23 个有效 commit，依次覆盖脚手架、Base32、三种哈希、HMAC、HOTP、TOTP、密钥生成、otpauth、CLI、示例、CI、文档、与已有项目关系说明、Steam/恢复码/重同步、审计修复、申报书排版等阶段，无空提交或拆分凑数；
- 已发布 mooncakes 包 `1726914517-spec/moon_otp@0.3.1`；
- 40 个测试全部通过，覆盖 HOTP Appendix D、TOTP Appendix B（SHA-1/256/512）、HMAC RFC 向量、Base32 RFC 4648、otpauth 往返、Steam、恢复码、重同步与容差校验。
