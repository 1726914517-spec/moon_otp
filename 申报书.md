## moon_otp 项目申报书

**基本信息**

- 项目名称：moon_otp：MoonBit 一次性密码库（HOTP / TOTP）
- 参赛者：（待填写）
- 联系方式：（待填写）
- GitHub 仓库链接：https://github.com/zhu-tianchi0210/moon_otp
- 项目方向：开发者工具 / 工具库
- 是否为移植项目：否（依据公开 RFC 标准原生实现，未复制任何第三方代码）

**项目简介**

moon_otp 用纯 MoonBit 实现两步验证（2FA）中最通用的一次性密码标准：事件型 HOTP（RFC 4226）和时间型 TOTP（RFC 6238），并补齐其全部依赖——HMAC（RFC 2104）、SHA-1/256/512（FIPS 180-4）、RFC 4648 Base32、随机密钥生成，以及 Google Authenticator 事实标准的 otpauth:// URI 构建与解析。

申报前已在 mooncakes.io 检索，TOTP/HOTP/OTP/Base32/otpauth 关键词均无重合项目，而 TOML、YAML、MessagePack 等方向已有成熟实现。一次性密码是登录、服务器登录、令牌硬件等场景的基础能力，任何 MoonBit Web 服务或 WASM 应用都可能需要；目前在 MoonBit 中使用只能外调 JS/宿主加密库，既无法在 WASM 后端独立运行，也要承担跨语言数据校验成本。本项目交付为可 `moon add` 的库加一个命令行工具，零外部依赖，可直接编译到 wasm。

**核心功能范围**

- RFC 4648 Base32 编解码（标准与 Extended Hex 字母表，padding 可选且严格校验）；
- SHA-1、SHA-256、SHA-512 哈希，HMAC 统一封装；
- RFC 4226 HOTP：8 字节大端计数、动态截断、位数与零填充可配；
- RFC 6238 TOTP：时间步长、位数、算法可配，提供当前时间便捷接口；
- 基于平台 CSPRNG 的随机密钥生成，输出无 padding 的 Base32 密钥串；
- otpauth://totp URI 的构建与完整解析（label issuer、percent 编解码、默认值与错误校验）；
- CLI 四个子命令：gen（生成密钥与二维码内容）、now、hotp、uri；
- 统一错误类型 OtpError，覆盖非法位数/周期、空密钥、随机源不可用、URI 格式错误等。

**预期验收产物**

- 发布到 mooncakes.io 的 moon_otp 库（包名 1726914517-spec/moon_otp），可 `moon add 1726914517-spec/moon_otp` 使用，也可 `moon run cmd/main` 使用 CLI；
- 32 个测试全部通过，覆盖 RFC 4226 Appendix D 完整序列、RFC 6238 Appendix B 三种算法全表（含 20000000000 等边界时间）、RFC 2202/4231 HMAC 向量、FIPS/NIST 哈希向量及 Base32 向量；
- 可运行示例 examples/totp_demo；GitHub Actions CI 覆盖 check/build/test；
- README 含可复现的构建、测试与 API/CLI 使用说明；Apache-2.0 许可证。

**参考说明**

- 参考标准：RFC 2104 / 4226 / 4648 / 6238、FIPS 180-4（均为公开规范，非代码）；
- SHA-512 轮常量与初始值与 Mozilla NSS 公开源码逐项交叉核对，实现为独立编写；
- 本项目许可证：Apache-2.0。
